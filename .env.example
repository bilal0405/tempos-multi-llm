# Copy this file to .env and fill in your API keys.
# Any provider whose key is missing will be skipped automatically.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=
GOOGLE_API_KEY=

# Optional: override the default model for each provider.
# Defaults are set in backend.py — leave blank to use them.
OPENAI_MODEL=
ANTHROPIC_MODEL=
XAI_MODEL=
DEEPSEEK_MODEL=
GOOGLE_MODEL=
